# Student-facing demo build. Deliberately contains NO Supabase URL or key —
# the mock client serves everything from memory, so this bundle cannot reach
# real student data. Build with:  npm run build:demo
VITE_DEMO_MODE=true
